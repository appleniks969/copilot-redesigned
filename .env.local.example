# GitHub Copilot Metrics Configuration

# Your GitHub Organization name
NEXT_PUBLIC_DEFAULT_ORG_NAME=your-org-name

# Default period for metrics display (in days - max 28 due to API limitation)
NEXT_PUBLIC_DEFAULT_METRICS_PERIOD_DAYS=28

# Time estimation for completions (seconds saved per suggestion)
NEXT_PUBLIC_SECONDS_PER_SUGGESTION=55

# Default team slugs (comma-separated, no spaces)
# IMPORTANT: Do not use "teams" as a team slug
NEXT_PUBLIC_DEFAULT_TEAM_SLUGS=frontend-team,backend-team,devops-team
